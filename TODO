* When node is being formatted and in 'pending' juju state, we should still
  show the "your node is being allocated" message
* urwid 1.1 upgrades
* Replace --config openstack.yaml hard coded path with an "autogenerated" one
  that is deleted after every juju deploy, e.g. https://pastebin.canonical.com/98588/
